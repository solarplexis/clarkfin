# Various UI enhancments (some needing back-end support)
1. Org Admin
  a. User Profile page. User can edit profile, and upload a profile picture
  b. User avatar in app bar should open a menu to include 'Edit Profile' (see #1 above) and 'Sign Out' options
  c. Org admin should be able to edit org name and give it an org profile picture (aka logo)
  d. Invites section on dashboard should have a + icon in the section header (right justified) with an aesthetically pleasing tooltip with 'Invite Student'
  e. Student Roster and Courses buttons should follow the same as 1.d above, removing redundant UI elements on the page

# All data should have corresponding CRUD APIs (for first class integration), with proper role-based access

